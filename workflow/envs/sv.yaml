name: structural-variant
channels:
  - conda-forge
  - bioconda
dependencies:
  # Alignment tools
  - bwa
  
  # Sort and index tools
  - samtools
  
  # Quality control tools  
  - fastqc
  
  # Structural variant calling tools
  - delly
  - bcftools
  - tabix